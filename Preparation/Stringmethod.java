import java.util.Scanner;

public class Stringmethod{
    public static void main(String[] args){
        // Scanner scan = new Scanner(System.in);
        String name = "Surya";
        // int length = name.length();
        // char letter = name.charAt(2);
        // int firstindex = name.indexOf("d");
        // int lastIndex = name.lastIndexOf("r");
        // int secondindex = name.indexOf("d",firstindex  + 5);  //  we want after that then we will follw odd sequence  
        // String upper = name.toUpperCase();
        // String lower = name.toLowerCase();
        // name = name.trim();    //to eleminate the space starting and finishing of the word
        // name = name.replace("d","e");
        // System.out.println(name.isEmpty());  //to check that the string is empty or not

        // System.out.println("enter your password:");
        // String password = scan.nextLine();

        // if(name.equalsIgnoreCase(password)){
        //     System.out.println("your name and password are same ");
        // }else{
        //     System.out.println("password set succefully");
        // }
        System.out.println(name);

        
    }
}